version: '3.8'

services:
  api:
    build: .
    ports:
      - "8011:8011"
    environment:
      - ALLOWED_ORIGINS=https://followthecredits.com,https://www.followthecredits.com
    volumes:
      - .:/app
    restart: unless-stopped
